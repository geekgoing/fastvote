# FastVote Frontend Environment Variables

# Backend URL for server-side API calls (Server Components)
BACKEND_URL=http://localhost:8000

# Backend URL for client-side API calls (local dev only)
# In production, leave empty - ingress routes /api to backend
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
